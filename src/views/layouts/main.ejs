<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-900 text-white">
    <!-- Navigation Bar -->
    <nav class="bg-gray-800 py-4 fixed bottom-0 left-0 w-full md:top-0 md:bottom-auto z-50"> <!-- Sticky for both mobile and desktop -->
        <div class="container mx-auto px-4 flex items-center justify-between md:justify-start">
            <!-- Far Left Text (Desktop) -->
            <h1 class="text-xl font-bold hidden md:block mr-20">Virtron Metaverse</h1> <!-- Visible only on desktop -->

            <!-- Icons -->
            <div class="flex items-center justify-around w-full md:w-auto md:space-x-20">
                <button>
                    <span class="text-gray-400 hover:text-white text-xl transition duration-200">🖼️</span>
                </button>
                <button>
                    <span class="text-gray-400 hover:text-white text-xl transition duration-200">🔍</span>
                </button>
                <button class="relative">
                    <span class="text-gray-400 hover:text-white text-xl transition duration-200">🔔</span>
                    <span class="absolute -top-1 -right-2 text-xs bg-red-500 text-white rounded-full px-1">
                        11
                    </span>
                </button>
                <button>
                    <span class="text-gray-400 hover:text-white text-xl transition duration-200">⚙️</span>
                </button>
            </div>
        </div>
    </nav>

    <!-- Page-Specific Content -->
    <main class="container mx-auto px-4 py-20 md:pt-20 md:pb-4"> <!-- Padding to prevent overlap -->
        <%- body %> <!-- Page-specific content will be injected here -->
    </main>
</body>
</html>
